Submission from prepared.ly
===============================================
A Prepared.ly user just submitted a question:
   
<%= @contact.name %>">
<%= @contact.email %>">
<%= @contact.subject %>">
<%= @contact.body %>">